import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    searchWrapper: {
        flexDirection: "row",
        borderWidth: 1,
        borderColor: "#000" ,
        borderRadius: 16,
        paddingHorizontal: 12,
    },
    searchInput: {
        flex: 1,
    }
})